# Архивы (старые версии — не коммитить, CI extract-zip удалён)
*.zip

# Системные
.DS_Store
node_modules/
*.log
.env

# SQLite БД (не коммитить — данные в проде)
extracted/server/data.db
extracted/server/data.db-wal
extracted/server/data.db-shm

# Сгенерированные страницы врачей (создаются publish-server из БД на проде).
# template.html — единственный отслеживаемый файл в каталоге.
extracted/vrachi/*.html
!extracted/vrachi/template.html
